# Web context configuration
#web.http.port=8181
#web.http.path=/api

# Database configuration
edc.datasource.default.url=jdbc:postgresql://localhost:5432/edc
edc.datasource.default.user=postgres
edc.datasource.default.password=password

# Or for H2 (in-memory):
# edc.datasource.default.url=jdbc:h2:mem:testdb
# edc.datasource.default.user=sa
# edc.datasource.default.password=

# Management API
web.http.management.port=8181
web.http.management.path=/management

# Control Plane API
web.http.control.port=8282
web.http.control.path=/control

# API auth (for now, disable for dev)
edc.api.auth.key=1234